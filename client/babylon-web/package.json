{
  "name": "@animal-zoom/babylon-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('no tests yet: @animal-zoom/babylon-web')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@animal-zoom/share": "workspace:*",
    "@babylonjs/core": "^7.27.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.8"
  }
}
