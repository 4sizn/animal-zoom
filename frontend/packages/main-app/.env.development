# Development Environment Configuration
# Animal Zoom - Main App

# API Configuration
# Backend API server URL - leave empty to use Vite proxy
VITE_API_URL=
# API prefix for Vite proxy (will be rewritten to root on backend)
VITE_API_PREFIX=/api

# WebSocket Configuration
VITE_WS_URL=http://localhost:3000

# Debug Mode
VITE_DEBUG=true

# Environment
NODE_ENV=development
