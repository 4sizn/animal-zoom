
2026-03-10
- Disambiguate actions by renaming room action from "Join room" -> "Enter room" (icon `door_open`) and making create card explicitly "Create a new room" with CTA "Create new room".
- Keep equal heights via existing `flex flex-col` + `mt-auto` CTA pinning.
