- 2026-03-06: The Zoom-like `/` experience can be safely reused by extracting shared room rendering into `pages/room/study/ZoomRoomExperience.tsx`; root keeps existing `participants` query behavior by resolving count from `window.location.search`.
- 2026-03-06: `pages/dashboard/data.ts` is a good source of deterministic room metadata; a minimal exported `getDashboardRoomById(roomId)` helper supports room-id-based participant derivation without touching dashboard UI behavior.
- 2026-03-06: A dedicated `RoomJoinPage` can stay side-effect-only by reading `joomId` from `useParams` and redirecting in `useEffect`; trimming the param and falling back to `/dashboard` safely handles missing or empty route values.
- 2026-03-06: `RoomCreatePage` can follow the same side-effect-only redirect pattern as join: generate a client id in `useEffect`, navigate with `{ replace: true }`, and keep a minimal dark-surface loading state while redirecting.
- 2026-03-06: Route registration for room flows belongs in `client/webapp/src/main.tsx`; adding `/room/study/:roomId`, `/room/join/:joomId`, and `/room/create` before the `*` redirect preserves root Zoom demo, auth, and dashboard behavior.
- 2026-03-06: Dashboard CTA wiring can stay layout-neutral by adding `useNavigate` in `client/webapp/src/pages/dashboard/index.tsx` and attaching `onClick` handlers only on existing buttons/cards (`/room/create` and `/room/join/${room.id}`).
