- Verified on 2026-02-27: docs/modules/client-babylon-web-spec.md exists; references found in docs/project-spec-for-agents.md and docs/modules/client-webapp-spec.md.

- 2026-02-28: src/main.ts now creates a single hidden working canvas (#babylon-working-canvas) and initializes exactly one Babylon Engine with one runRenderLoop for all routes.
- 2026-02-28: Route renders now call clearViews() before DOM replacement, then register route canvases with engine.registerView(...) after mount to prevent stale view accumulation across /solo, /room, /my-room.
- 2026-02-28: Added defensive per-route canvas lookup so missing canvases skip registration and avoid runtime crashes while scenes are not wired yet.
- 2026-02-28: Added reusable Babylon scene factories at client/babylon-web/src/scene/sceneFactory.ts for single-view and participant-view bundles; both return { scene, camera, dispose } with explicit camera.detachControl() + scene.dispose() ownership.
- 2026-02-28: Scene factories keep controls detached by default and only attach when options.attachControl=true with options.inputElement, allowing /my-room interactivity later without changing /solo or /room behavior now.
- 2026-02-28: Route lifecycle now treats /solo scene ownership explicitly: on route leave it disposes the solo SceneBundle (camera detach + scene dispose), then unregisters route views, and on route enter it creates/registers one solo scene+camera pair; this prevents stale Babylon resources while keeping one shared Engine/runRenderLoop.
- 2026-02-28: /room now keeps a `Map<HTMLCanvasElement, { participantId, bundle, view }>` so `engine.activeView` can be resolved to its `target` canvas and used as the lookup key for rendering exactly that participant scene in the shared render loop.
- 2026-02-28: /room cleanup is split into route-exit disposal (unregister each participant canvas, dispose bundle, remove canvas element, remove room button listeners) and global `clearViews()` as a safety net, which avoids leaked views/listeners when navigating away.
- 2026-02-28: /room participant views now use `IntersectionObserver` to flip each Babylon `view.enabled` based on canvas visibility; unsupported environments default to enabled, removals unobserve per-canvas, and route exit disconnects the observer to prevent leaks.
- 2026-02-28: `/my-room` now creates a real controls panel (`#my-room-theme-select`, `#my-room-avatar-select`) and wires change listeners on route entry; handlers immediately mutate the active single-view scene by updating `scene.clearColor`, `single-view-light.intensity`, and `single-view-avatar-proxy` material/scaling.
- 2026-02-28: `/my-room` rendering is crash-safe: missing canvas short-circuits registration, scene init is wrapped in try/catch, and render-loop errors disable only the my-room render flag instead of crashing the shared engine runtime.
- 2026-02-28: `/my-room` route exit now performs explicit cleanup via `disposeMyRoomScene()` (remove selector listeners + dispose SceneBundle), while global `clearViews()` continues to unregister the route canvas view to avoid leaks when leaving the page.
