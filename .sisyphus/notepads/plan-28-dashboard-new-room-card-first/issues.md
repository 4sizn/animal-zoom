
2026-03-10
- Search-mode note: `task(subagent_type="explore"|"librarian", run_in_background=true)` returns Task IDs but background retrieval fails ("Task not found"). Used direct `grep`/`read`/`ast-grep` instead.
