# Decisions

- Added `worktree_path` in `.sisyphus/boulder.json` using the existing main worktree path to avoid unnecessary branch/worktree churn.
- Chose `docs/project-spec-for-agents.md` as the primary agent-facing specification document for project intent, requirements, and acceptance criteria.
- Kept requirement statements grounded in currently implemented code and scripts only.
