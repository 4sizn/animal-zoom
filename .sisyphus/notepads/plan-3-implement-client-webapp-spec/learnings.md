
2026-03-01: FR-WEB-4 scaffold: add `rxjs` to `client/webapp` and subscribe to an Observable in `App()` via `useEffect`, ensuring no visual impact.

2026-03-01: Hands-on QA workaround: start dev server in background and use `pnpm dlx playwright screenshot` to capture `.sisyphus/artifacts/plan-3-implement-client-webapp-spec/*.png`, then compare to `client/webapp/example/*/screen.png`.
