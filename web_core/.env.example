# API Server Configuration
# Development: http://localhost:3000
# Production: https://api.yourdomain.com
VITE_API_URL=

# WebSocket Configuration
# Development: http://localhost:3001
# Production: wss://ws.yourdomain.com
VITE_WS_URL=

# Optional: API prefix (e.g., /api/v1)
VITE_API_PREFIX=

# Application
VITE_APP_NAME=Animal Zoom
VITE_APP_VERSION=1.0.0

# Asset Management Configuration
# Mode: local (development) | s3 (production with presigned URLs) | cdn (production with CDN)
# Development: use 'local' to serve assets via API server
# Production: use 's3' for presigned URLs or 'cdn' for CDN
VITE_ASSET_MODE=local

# CDN Base URL (only required when VITE_ASSET_MODE=cdn)
# Example: https://cdn.yourdomain.com
VITE_CDN_BASE_URL=

# Debug Mode
# Set to 'true' to enable API request/response logging
VITE_DEBUG=false
